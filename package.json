{
  "name": "tournament-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "api": "npm start --prefix api",
    "api-install": "npm install --prefix api",
    "client": "npm start --prefix client",
    "client-install": "npm install --prefix client",
    "script-install": "npm install --prefix script",
    "script": "npm start --prefix script",
    "start": "concurrently \"npm run api\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.0.2"
  }
}
